{
  "name": "backend",
  "version": "0.0.0",
  "description": "",
  "main": "server.js",
  "engines": {
    "node": "6.x.x",
    "npm": "4.x.x"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "deploy": "mkdir -p dist && cp server.js dist && cp package.json dist && cp Procfile dist && cf push"
  },
  "author": "Ruben Laube-Pohto",
  "license": "MIT",
  "dependencies": {
    "body-parser": "^1.17.1",
    "cors": "^2.8.3",
    "express": "^4.15.2",
    "jsonwebtoken": "^7.3.0",
    "passport": "^0.3.2",
    "passport-jwt": "^2.2.1"
  }
}
